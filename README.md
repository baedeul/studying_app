# 학습 시간 카운팅 앱

브라우저에서 동작하는 간단한 학습 시간 카운터입니다.

## 기능

- 시작/일시정지/초기화 버튼으로 학습 시간 측정
- 일시정지 시 세션 기록 자동 저장
- 오늘의 총 학습 시간 합산 표시
- 브라우저 `localStorage`로 기록 유지

## 실행 방법

정적 파일이라 별도 빌드 없이 실행됩니다.

```bash
python3 -m http.server 4173
```

브라우저에서 `http://localhost:4173` 접속
